# code editor

Remove the top tab saying Workspace. the plugin dropdown must move to the Workspace files and under each plugins the folder structure must come.

Remove the keyboard shortcuts section altogether from the editor panel.

rmove the expand close of the dependencies. make it visible. But the buttons and checkboxes and dropdown should foloow the overall theme and conventions. Instead of long names put icons, and on hover display the names.

suppport for formatting (.prettierc should be respecting) in editor options allow to toggle format on save.