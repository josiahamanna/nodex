# Active web backend host:port — managed by scripts/docker-web-deploy.sh / docker-web-swap.sh
# Default matches the slot Compose starts first (nodex-web-blue).
set $nodex_web_backend "nodex-web-blue:3000";
