# Full upstream block for UI — managed by scripts/docker-web-deploy.sh
# Default must match the slot Compose starts first (nodex-web-blue) or nginx fails at gateway start
# ("host not found in upstream") before green exists.
upstream nodex_web {
    server nodex-web-blue:3000;
}
