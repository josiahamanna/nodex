/** Dispatched with `detail: { noteId: string }` so the matching markdown editor opens the note link picker. */
export const NODEX_MARKDOWN_OPEN_NOTE_LINK_PICKER_EVENT = "nodex:markdown-open-note-link-picker" as const;
