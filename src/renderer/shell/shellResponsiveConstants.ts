/** Minimum viewport width (CSS px) for the desktop-only gate (~12" at 96 CSS px/in). */
export const DESKTOP_MIN_VIEWPORT_WIDTH_PX = 12 * 96;

/** When the companion column is narrower than this (px), auto-collapse it (collapse-only on viewport shrink). */
export const SHELL_COLLAPSE_COMPANION_MIN_PX = 200;

/** When the left dock (activity bar + side panel) is narrower than this (px), auto-collapse it. */
export const SHELL_COLLAPSE_LEFT_DOCK_MIN_PX = 220;
