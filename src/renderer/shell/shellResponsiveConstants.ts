/** Fixed width (CSS px) of the activity bar (menu rail) when visible — fits `h-9` controls with margin. */
export const SHELL_ACTIVITY_BAR_WIDTH_PX = 48;

/** Minimum expanded width (CSS px) for the side panel column; also used for shrink-triggered auto-collapse. */
export const SHELL_SIDEBAR_MIN_EXPANDED_PX = 200;

/** Minimum expanded width (CSS px) for the companion column; also used for shrink-triggered auto-collapse. */
export const SHELL_COMPANION_MIN_EXPANDED_PX = 200;
