/** IndexedDB name for browser-local WPN scratch data (web try-out, no login). */
export const WPN_SCRATCH_INDEXEDDB_NAME = "nodex_wpn_scratch__v1";

/** Single logical owner for scratch rows (not exposed in DTOs). */
export const WPN_SCRATCH_OWNER_ID = "__web_scratch__";
