import videoJsCss from "video.js/dist/video-js.css?raw";

/** Full Video.js stylesheet for plugin srcdoc iframes (host webpack CSS does not cross into the iframe). */
export const VIDEO_JS_IFRAME_CSS = videoJsCss;
